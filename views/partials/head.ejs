<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title><%= title %></title>
</head>
<body>

    <nav class="navbar">
        <div class="container">
            <a class="navbar-brand" href="#">Task Manager</a>
            <button class="menu-toggle" onclick="toggleMenu()">☰</button>
            <ul class="nav-links" id="navLinks">
                <li><a href="/">Home</a></li>
                <% if (user) { %>
                    <li><a href="/auth/sign-out">Sign Out</a></li>
                    <li><a href="/users/<%= user._id %>/Tasks/new">Add a New Task</a></li>
                    <li><a href="/users/<%= user._id %>/Tasks">View All Tasks</a></li>
                <% } else { %>
                    <li><a href="/auth/sign-up">Sign Up</a></li>
                    <li><a href="/auth/sign-in">Sign In</a></li>
                <% } %>
            </ul>
        </div>
    </nav>

    <script>
        function toggleMenu() {
            document.getElementById("navLinks").classList.toggle("active");
        }
    </script>

</body>
</html>
